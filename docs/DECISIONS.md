# Decision log

| ID | Decision | Status | Reason |
|---|---|---|---|
| D-01 | The SQLite ledger is a **rebuildable cache** of Walrus, never the only source | Proposed | Official rule: memory stored exclusively on Walrus. The restore test (delete SQLite → `restore` → same state) proves compliance |
| D-02 | Do not use `analyze`; write structured facts with `remember` | Accepted | Preserves type, id and supersession |
| D-03 | Current state is resolved in code, not by an LLM nor by plain recall | Accepted | Determinism; recall can come back empty |
| D-04 | Channel: Telegram first; the core is channel-agnostic so WhatsApp (Baileys) can be added later | Accepted | Bot created (@Palavra_paradevs_bot), privacy mode on. WhatsApp via Baileys carries ban risk and unreliable buttons |
| D-05 | Single LLM: Gemini, model **`gemini-2.5-flash-lite`** (the model configured in the team `.env`), via the **`@google/genai`** SDK (Node). `GEMINI_MODEL` stays configurable | Accepted (changed on 2026-09-24) | Supersedes the earlier proposal `gemini-3.8-flash` (PR #1). Eligible: the official rules list "Google Gemini" without restricting the version. `gemini-2.5-flash-lite` is the only model that completed the extraction check: 100% valid JSON, 100% type accuracy and 100% owner accuracy on the first 16 cases; `gemini-3.8-flash` returned 403 and then 503 in every attempt. Only 2 of 9 due dates were wrong, which the prompt refinement should fix. The Google account is the one that owns the API key; it was created by Maria (Mari). Whether billing is enabled on it is still to be confirmed: the free tier uses prompts for training and hit its quota (429). Transient 503/429 errors are handled with retry and backoff (see `eval/gemini-extraction.ts`); a fallback model is still open Fallback rule (2026-09-25): `GEMINI_FALLBACK_MODEL` (`gemini-3.5-flash`) takes over when the primary keeps failing (503/429) or returns invalid output; transient errors are retried on the same model after 0.6 s, 2 s and 3 s first. Verified live on 2026-09-25: the schema with a date pattern is accepted and 3 of 3 messages returned correct dates (about 1 s each). |
| D-06 | Namespace `grp:<chat_id>` under one bot account | Accepted | Isolation is per owner + namespace |
| D-07 | One submission per team | Official rule | Define who submits and which wallet receives the prize |
| D-08 | English everywhere in the repository, and **the bot replies in English** | Accepted (changed on 2026-09-24) | International hackathon and international judges; supersedes both "bot in PT-BR" and "bot answers in the user's language". Group members may still write in Portuguese; the extractor understands both (see `eval/fixtures/`) |
| D-09 | New public repository at `github.com/Astreus-J/palavra` | Done | Public repo is mandatory |
| D-10 | Promo Prize: promote in spaces not affiliated with Walrus/Sui | New | X and r/sui do not count |
| D-11 | English fact types (`DECISION`, `COMMITMENT`, `AMENDMENT`, `COMPLETION`) and English commands (`/palavra`, `/pending`, `/history`, `/decisions`) | Accepted | Follows D-08 |
